JAVA开发中为什么经常把JSON字符串反序列化为对应的结构化实体而不直接转JSONObject对象：

1.当目标数据只使用一次时，不需要定义对应实体类，JSON字符串 直接反序列化为 JSONObject对象，层层获取到目标数据。
2.当需要多次使用JSON数据时，通过定义对应实体类，再反序列化为结构化的实体对象，对于强类型的Java语言，由于定义的实体对象有类型，转化后的结构化数据更直观，使用起来更方便。
3.归总方案：多次使用的数据可局部获取转化为结构化实体对象，一次使用的数据可直接层层获取。

